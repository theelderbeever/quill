# quill-engine

Connection registry and query lifecycle. Routes single-connection queries direct to the driver, multi-connection queries through DataFusion + federation. The GUI's only backend dependency.
